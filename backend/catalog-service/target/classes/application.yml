server:
  port: ${SERVER_PORT:8081}

spring:
  application:
    name: catalog-service
  datasource:
    url: ${POSTGRES_URL:jdbc:postgresql://localhost:5432/shopcloud_catalog}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

shopcloud:
  jwt:
    secret: ${SHOPCLOUD_JWT_SECRET:change-me-to-a-very-long-secret-key-32-chars-minimum}
  internal-token: ${SHOPCLOUD_INTERNAL_TOKEN:change-me-internal-token}

management:
  endpoints:
    web:
      exposure:
        include: health,info

