# The customer frontend uses relative API paths by default.
# Vite proxy forwards these paths to the correct Spring Boot service during local development.
# In EKS, use ingress path routing with the same /api/... paths.
